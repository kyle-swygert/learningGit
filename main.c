#include <stdio.h>

int main(void) {

    // print Hello World 15 times and the iteration number. 
    for (int i = 0; i < 15; ++i) {
        printf("Hello World: %d\n", i);
    }

    return 0;
}